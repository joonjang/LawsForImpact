<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LawsForImpact.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ScrollView>
    <StackLayout Spacing="20" Padding="15">
        
        <Label Text="{Binding LawOrPrinciple}" 
               d:Text="Rule or Law" 
               HorizontalOptions="CenterAndExpand" 
               FontSize="20" 
               FontAttributes="Bold,Italic" />
        <Label Text="{Binding HeaderTitle}" d:Text="Item name" FontAttributes="Bold"/>
        <Label Text="{Binding HeaderDescription}" d:Text="Item description"/>
        <Button VerticalOptions="EndAndExpand" Text="Refresh" Clicked="Button_Clicked"/>

        

        </StackLayout>
    </ScrollView>


</ContentPage>